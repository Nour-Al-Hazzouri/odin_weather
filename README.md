# Weather App

This repo is a simple weather app that uses the OpenWeatherMap API to fetch weather data for a given city. It uses webpack to bundle the code and a simple HTML structure to display the weather data.
